# Quant Validation Engine v0.1.0

**量化策略科学验证引擎** —— 一个拒绝了一切声称有 Alpha 的策略的框架。

> ⚠️ 这不是"AI 交易机器人"，也不是"自动赚钱工具"。
> 这是一个**验证引擎**：它把策略放在四道铁律下审判，输出 PASS / REJECT。

---

## 一句话定位

**我们做了一切"野生 Alpha 挖掘机"的噩梦**——一个把策略放在四道铁律下审判、并且诚实地拒绝它们的框架。

我们用它在 300+ 候选、3 个风险状态假设、45 个多周期组合上只得到过 **0 个 PASS**。

**这不是缺陷。这就是它工作的方式。**

---

## 为什么开这个项目

大多数量化项目的死法不是"不会赚钱"，而是：

```
策略亏损 → 改参数 → 加Agent → 加过滤器 → 再回测 → 漂亮曲线 → 上实盘 → 亏钱
```

每一环都在制造"看起来能赚钱"的假象。

本引擎提供相反的路径：

```
提出假设 → 四铁律验证 → REJECT/PASS → 归档 → 换机制
```

---

## v0.1.0 包含什么

| 模块 | 文件 | 作用 |
|---|---|---|
| 四铁律核心 | `core/four_laws.py` | 零依赖、独立可复用的验证引擎 |
| 策略层 | `strategy_layer/` | 生成 / 预算 / 验证 / 盲测 四段管道 |
| 第一个示例 | `examples/rsi_reversal.py` | **故意用失败策略**展示引擎如何 REJECT |
| 贡献指南 | `CONTRIBUTING.md` | 贡献验证纪律，不是贡献赚钱策略 |
| 安全策略 | `SECURITY.md` | 密钥处理 + 免责声明 |

---

## 上手 3 条命令

```bash
# 1. 回归测试: 验证引擎自身正确(复跑已知 REJECT 族, 判决必须不变)
python strategy_layer/orchestrator.py --regression

# 2. 探索一批候选(生成 → 预算 → 验证 → 记账)
python strategy_layer/run_explore.py --n 10 --n-twin 24

# 3. 多周期扩展(回答"这个周期没信号是周期问题还是机制问题")
python strategy_layer/multi_tf_explore.py --n-twin 100
```

---

## 第一个示例: 看一个策略如何被杀死

```python
# examples/rsi_reversal.py
# 一个经典的"看起来有效"的 RSI 反转策略
# 运行四铁律 → REJECT (z=-1.12, 正折比 0.36, OOS 负期望)
# 你会看到: 为什么它过不了孪生对照(随机也会"赚")
```

**我们刻意用"失败策略"做第一个示例**——不是展示"赚钱曲线"，而是展示"验证引擎如何诚实地拒绝一个策略"。这建立了可信度：**框架不骗你**。

---

## 四铁律（验证标准，永不降低）

1. **分布相交 + OOS 净期望 > 0**：信号分布与价格分布在样本外须可区分，且扣除成本后期望为正
2. **等率随机对照（孪生）**：block-shuffle 置换零检验，\|z\|≥2 才过；唯一差异 = 信号-价格时间对齐
3. **前视控制**：walk-forward 多折，而非单一固定切分
4. **功效反向**：加折 z 应升，反降回 50% = 无效应

---

## 贡献者

本版本由 **吴文彬 (@kaiwenwo17-cmd)** 初始发布。

欢迎通过 Pull Request / Issue 参与（详见 `CONTRIBUTING.md`）。贡献类型包括但不限于：
- 新机制模板（机制族 30 个目前只实现了部分）
- 新状态变量（风险实验的输入）
- 协议改进 / 挑战框架某道闸（过松或过严都欢迎讨论）

---

## 信号追求线重启条件

本框架不预设"存在第一个 Alpha"。只有当以下**任一**条件成立时才考虑重开信号追求：

1. 出现**新信息类数据**（逐笔成交 / 做市商盘口深度）
2. 切换**市场模式**（跨所 carry / 资金费率结构套利）
3. 提出**明确机制 + 信息优势**的新假设（非参数组合穷举）

即使在那一刻，新假设也必须过完整的四铁律才能被接受。

---

## 免责声明

本引擎仅用于**研究 / 验证 / 教学**。它不提供投资建议，不承诺任何收益，不构成交易依据。任何基于本框架产出的"策略"在上实盘前都必须自行承担风险并满足当地法规。

---

*License: MIT*
