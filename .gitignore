# ── 密钥/凭证(永不入库) ──
.env
.env.*
*.key
*.secret
*secret*
*SECRET*
credentials.json
api_keys.json
authz_tokens.json

# ── 数据(不入库, 用户自备) ──
data/
*.csv
*.jsonl
!examples/*.json

# ── Python ──
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# ── 运行时状态 ──
*.log
*.pid
*.lock
